#include <stdio.h>
int main (){
	int x;
	int y;
	int z;
	int a;
	printf("valor da variavel x:",x);
	scanf("%d, &x");
	printf("valor da variavel y:",y);
	scanf("%d, &y");
	printf("outro valor:",z);
	scanf("%d, &z");
	print("valor é: %d",a);
	a = (x + y) / z;
	return 0;
}
